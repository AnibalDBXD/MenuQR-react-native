import React from 'react';
import { Text } from 'react-native';
import { MenuProps } from './types';

const Menu: React.FC<MenuProps> = (): JSX.Element => {
  return <Text>Memnum</Text>;
};

export default Menu;
