import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  MenuListContainer: {
    marginLeft: 'auto',
    marginRight: 'auto',
    paddingBottom: 60,
  },
});

export default styles;
